Start the program.

Initialize totalLoad to 0.

Use a while loop to continuously accept appliance power ratings.

Add each rating to totalLoad.

Check if totalLoad exceeds 3000 W.

If yes, display “Load limit exceeded” and stop adding more appliances.

Display the final total load.

End the program.